"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const rule_tester_1 = require("@typescript-eslint/rule-tester");
const no_context_sync_in_loop_1 = __importDefault(require("../../src/rules/no-context-sync-in-loop"));
const ruleTester = new rule_tester_1.RuleTester({
    parser: '@typescript-eslint/parser',
});
ruleTester.run('no-context-sync-in-loop', no_context_sync_in_loop_1.default, {
    valid: [
        {
            code: "context.sync()"
        },
        {
            code: `Excel.run(async (context) => { 
        context.sync(); 
      });`
        }
    ],
    invalid: [
        {
            code: `Word.run(async (context) => { 
          for(i = 0; i < 5; i++) { context.sync(); } 
        });`,
            errors: [{ messageId: "loopedSync" }]
        },
        {
            code: `Excel.run(async (context) => { 
          var person = { fname:\"John\", lname:\"Doe\", age:25 }; 
          var x; 
          for(x in person) { context.sync(); } 
        });`,
            errors: [{ messageId: "loopedSync" }]
        },
        {
            code: `PowerPoint.run(async (context) => { 
          var cars = ['BMW', 'Volvo', 'Mini']; 
          var x; 
          for(x of cars) { context.sync(); } 
        });`,
            errors: [{ messageId: "loopedSync" }]
        },
        {
            code: `Word.run(async (context) => { 
          while(true) { context.sync() } 
        });`,
            errors: [{ messageId: "loopedSync" }]
        },
        {
            code: `Excel.run(async (context) => { 
          do { context.sync() } while(true); 
        });`,
            errors: [{ messageId: "loopedSync" }]
        }
    ]
});
//# sourceMappingURL=no-context-sync-in-loop.test.js.map